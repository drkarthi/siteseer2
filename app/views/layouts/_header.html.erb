<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">  

      <%= link_to "siteseer", '#', id: "logo" %>

      <nav>
        <ul class="nav pull-right">
          <li class ="horizontal"><%= link_to "Home",    home_path %></li>
          <% if !user_signed_in? %>
          <li class ="horizontal_link"><%= link_to "Sign in", new_user_session_path %></li>
          <% end %>
          <li class ="horizontal"><%= link_to "Search", search_path %></li>
          <% if user_signed_in? %>
          <li class ="horizontal"><%= link_to "Add location", add_path %></li>
          <% end %>
          <li class ="horizontal"><%= link_to "Help",    help_path %></li>  
          <li id="user_nav">
          <% if user_signed_in? %>
          Signed in as <%= current_user.email %>. Not you?
          <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
          </li>
        <% end %>         
        </ul>
      </nav>
    </div>
  </div>
</header>